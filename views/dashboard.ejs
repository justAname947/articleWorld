
<% if (error != '') { %>
    <div class="alert alert-warning alert-dismissible fade show mt-3" role="alert">
        <%= error %>
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>  
 <% } %>
<h1 class="mt-4">Dashboard</h1>
<p>Welcome <%= user.name.charAt(0).toUpperCase() + user.name.slice(1) %></p>

<h3 class="mt-3">Breaking news!</h3>

<ul class="list-group">
    <% articles.reverse().forEach(article =>{ %>
        <li class="list-group-item"><a href="/articles/<%=article._id%>"><%= article.headline %></a></li>
      <% }); %>    
</ul>

  <!--  
      <% articles.forEach(article =>{ %>
        <li class="list-group-item"><%= article.headline %></li>
      <% }); %>
 -->